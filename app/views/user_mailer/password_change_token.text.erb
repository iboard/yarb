
PLEASE CHANGE YOUR PASSWORD

You just requested a password change at <%= Settings.fetch( :app, :hostname ) -%>.
Please confirm this by clicking the link below.

<%= edit_password_users_url(token: @token) %>


BITTE KENNWORT ÄNDERN

Du hast gerade eine Kennwortänderung bei <%= Settings.fetch( :app, :hostname) -%> beantragt.
Bitte wähle ein neues Passwort durch einen Klick auf den Link.

<%= edit_password_users_url(token: @token) %>


