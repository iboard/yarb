%li{ id: "page-#{page.to_param}", class: 'list-entry bottom-bordered' }
  = link_to t(:delete), page_path(page), method: :delete, data: { confirm: t(:are_you_sure) }, class: 'btn btn-danger pull-right'
  = link_to page.title, page_path(page)
