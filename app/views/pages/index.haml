- if has_roles? %i(admin author maintainer editor)
  .pull-right
    =link_to icon('icon-plus-sign icon-white')+t(:add_new_page), new_page_path, class: 'btn btn-success'

%h1=t(:listing_of_pages)

%ul#pages-list.item-list.big.top-bordered
  = render @pages
